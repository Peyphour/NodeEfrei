routes:

-   / => index: create username
-   /home: list channels or create channel
-   /{channel}: chat on channel, still display list of channels to join


- /chat/user-exists: returns false if username already exists
- /chat/register-user: register user
- /chat/list-channels
- /chat/create-channel

- /ws/{channel}